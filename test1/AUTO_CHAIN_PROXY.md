# 自动添加链式代理配置指南

## 🎉 好消息！

现在您**不需要手动添加**链式代理节点了！我已经修改了模板文件，可以通过 `pref.ini` 配置自动添加链式代理。

## 📝 配置步骤

### 方法一：通过 pref.ini 配置（推荐）

1. **打开 `pref.ini` 文件**

2. **找到 `[template]` 部分**，取消注释并修改以下配置：

```ini
[template]
;链式代理配置（Chain Proxy Configuration）
;启用链式代理：设置为 true 以启用链式代理节点
chain_proxy.enable=true

;链式代理节点名称（Chain Proxy Node Name）
chain_proxy.name=香港落地

;基础节点名称（Base Node Name）- 必须与订阅源中的节点名称完全匹配
chain_proxy.dialer_proxy=🇭🇰 香港节点

;链式代理服务器配置（Chain Proxy Server Configuration）
chain_proxy.type=ss
chain_proxy.server=23.175.201.164
chain_proxy.port=80
chain_proxy.cipher=2022-blake3-aes-128-gcm
chain_proxy.password=UETm2mAIRiCaVJuIe1t0cA==
chain_proxy.udp=true
```

3. **保存文件并提交到 GitHub**

4. **使用订阅链接获取配置**
   - 访问您的订阅链接
   - 链式代理节点会自动包含在生成的配置中！

### 方法二：通过 URL 参数配置

您也可以在订阅链接中添加参数来配置链式代理：

```
https://api.dler.io/sub?target=clash&url=...&config=https://raw.githubusercontent.com/heicat1337/sub/refs/heads/main/pref.ini&chain_proxy.enable=true&chain_proxy.name=香港落地&chain_proxy.dialer_proxy=🇭🇰%20香港节点&chain_proxy.server=23.175.201.164&chain_proxy.port=80&chain_proxy.cipher=2022-blake3-aes-128-gcm&chain_proxy.password=UETm2mAIRiCaVJuIe1t0cA==
```

## ⚙️ 配置参数说明

| 参数 | 说明 | 必需 | 默认值 |
|------|------|------|--------|
| `chain_proxy.enable` | 是否启用链式代理 | 是 | `false` |
| `chain_proxy.name` | 链式代理节点名称 | 是 | `香港落地` |
| `chain_proxy.dialer_proxy` | 基础节点名称（必须与订阅源中的节点名称完全匹配） | 是 | `🇭🇰 香港节点` |
| `chain_proxy.type` | 代理类型 | 是 | `ss` |
| `chain_proxy.server` | 服务器地址 | 是 | `23.175.201.164` |
| `chain_proxy.port` | 端口 | 是 | `80` |
| `chain_proxy.cipher` | 加密方式 | 是 | `2022-blake3-aes-128-gcm` |
| `chain_proxy.password` | 密码 | 是 | `UETm2mAIRiCaVJuIe1t0cA==` |
| `chain_proxy.udp` | 是否支持 UDP | 否 | `true` |

## ⚠️ 重要提示

1. **节点名称匹配**
   - `chain_proxy.dialer_proxy` 的值必须与订阅源中的节点名称**完全匹配**
   - 包括 emoji、空格、大小写等
   - 建议先获取一次订阅配置，查看实际的节点名称

2. **启用配置**
   - 必须设置 `chain_proxy.enable=true` 才会添加链式代理节点
   - 如果设置为 `false` 或不设置，链式代理节点不会被添加

3. **URL 编码**
   - 如果通过 URL 参数配置，需要对特殊字符进行 URL 编码
   - 例如：`🇭🇰 香港节点` 需要编码为 `🇭🇰%20香港节点`

## 🔄 工作流程

1. **配置 pref.ini**（或通过 URL 参数）
2. **访问订阅链接**
3. **subconverter 自动处理**
   - 读取订阅源节点
   - 根据配置添加链式代理节点
   - 生成完整的 Clash 配置
4. **直接使用** - 无需手动编辑！

## 📋 完整示例

### pref.ini 配置示例

```ini
[template]
chain_proxy.enable=true
chain_proxy.name=香港落地
chain_proxy.dialer_proxy=🇭🇰 香港节点
chain_proxy.type=ss
chain_proxy.server=23.175.201.164
chain_proxy.port=80
chain_proxy.cipher=2022-blake3-aes-128-gcm
chain_proxy.password=UETm2mAIRiCaVJuIe1t0cA==
chain_proxy.udp=true
```

### 生成的配置示例

```yaml
proxies:
  # 订阅源中的节点...
  - name: "🇭🇰 香港节点"
    type: ss
    # ...
  
  # 自动添加的链式代理节点
  - name: "香港落地"
    dialer-proxy: "🇭🇰 香港节点"
    type: ss
    server: "23.175.201.164"
    port: 80
    cipher: "2022-blake3-aes-128-gcm"
    password: "UETm2mAIRiCaVJuIe1t0cA=="
    udp: true
```

## 🎯 优势

✅ **无需手动编辑** - 配置一次，自动生成  
✅ **支持订阅更新** - 每次获取订阅都会自动包含链式代理  
✅ **灵活配置** - 可以通过 pref.ini 或 URL 参数配置  
✅ **易于维护** - 配置集中管理

## 🔧 故障排除

### 问题：链式代理节点没有出现

**解决方案**：
1. 检查 `chain_proxy.enable` 是否设置为 `true`
2. 检查所有必需参数是否都已配置
3. 确认 `chain_proxy.dialer_proxy` 的值与订阅源中的节点名称完全匹配

### 问题：dialer-proxy 节点不存在

**解决方案**：
1. 先获取一次订阅配置，查看实际的节点名称
2. 复制完整的节点名称（包括 emoji 和空格）
3. 更新 `chain_proxy.dialer_proxy` 的值

### 问题：配置不生效

**解决方案**：
1. 确认 pref.ini 文件已提交到 GitHub
2. 确认订阅链接中的 config 参数指向正确的 pref.ini 文件
3. 清除浏览器缓存后重试

